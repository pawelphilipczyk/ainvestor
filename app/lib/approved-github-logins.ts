/**
 * GitHub usernames allowed to use gist-backed features and AI advice.
 *
 * Matching is case-insensitive. When this list is **empty**, every successful
 * OAuth user is allowed (same as before an allowlist existed).
 *
 * To get access: add your GitHub login here and open a pull request.
 */
export const APPROVED_GITHUB_LOGINS: readonly string[] = []
