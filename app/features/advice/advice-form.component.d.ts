import type { EntryComponent } from '@remix-run/component'

/** Client entry for advice cash field + submit sync (see advice-form.component.js). */
export declare const AdviceFormEnhancement: EntryComponent
